{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tasks",
  "description": "Tasks View closes itself on dismiss and is dragged by its own title bar; the two resident windows must never be able to do either",
  "windows": [
    "tasks"
  ],
  "permissions": [
    "core:window:allow-close",
    "core:window:allow-start-dragging"
  ]
}
