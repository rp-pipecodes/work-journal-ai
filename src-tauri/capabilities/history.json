{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "history",
  "description": "History closes itself on dismiss and is dragged by its own title bar; the capture window must never be able to do either",
  "windows": [
    "history"
  ],
  "permissions": [
    "core:window:allow-close",
    "core:window:allow-start-dragging"
  ]
}
