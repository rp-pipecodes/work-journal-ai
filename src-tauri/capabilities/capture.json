{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "capture",
  "description": "Capture grows by one row per Project Prediction; it must never gain core:window:allow-close",
  "windows": [
    "capture"
  ],
  "permissions": [
    "core:window:allow-set-size"
  ]
}
