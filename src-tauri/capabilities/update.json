{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "update",
  "description": "Settings is the only place an update is looked for, downloaded and restarted into; the two resident panels have no screen to say any of it on",
  "windows": [
    "main"
  ],
  "permissions": [
    "updater:default",
    "process:allow-restart"
  ]
}
