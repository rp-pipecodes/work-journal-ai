/**
 * The Filter's day axis in words, for the one control that carries it.
 * Defined in the journal core, beside the other day formatters, so the
 * renderings built from the same axis — Review Material's heading — read in
 * exactly these words rather than in a copy of them.
 */
export { formatDayRange } from '@/journal/journal'
